<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework-message-queue:etc/publisher.xsd">
    <!-- This is for catalogue sync to publish messages in the right type -->
    <publisher topic="couture.catalogue.sync">
        <connection name="amqp" exchange="magento"/>
    </publisher>

    <!-- This is for order history sync -->
    <publisher topic="couture.order.sync">
        <connection name="amqp" exchange="magento"/>
    </publisher>

</config>
